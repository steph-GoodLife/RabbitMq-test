{% extends 'base.html.twig' %}

{% block title %}Test de mise en queue - RabbitMq!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Test de mise en queue - RabbitMq! ✅</h1>

    <h2>Voyons si Symfony sait compter:</h2>
    <h4>Entrez une addition</h4>
    
    <div class="example-wrapper">
        {{form(form) }}
    </div>

    <hr>

    <div class="example-wrapper">
        <h5>Resultat :</h5>
            {% for results in result %}
                {{ results }}
            {% endfor %}
    </div>

</div>
{% endblock %}
